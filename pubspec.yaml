name: the_time_chart
description: "Time Chart — Track, Improve, and Manage cycles, habits, actions & tasks"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.

  cupertino_icons: ^1.0.8
  timezone: ^0.9.4
  pausable_timer: ^3.1.0+3    #  snackbar
  get: ^4.7.2    # MediaPicker
  intl: ^0.20.2  #  DateFormatting
  share_plus: ^7.2.1  # For sharing functionality
  go_router: ^17.0.0  #  AppRouter
  image_picker: ^1.2.0   # MediaPicker
  video_player: ^2.8.2  # DisplayMedia
  file_picker: ^10.3.6  # MediaPicker
  crop_your_image: ^2.0.0  # or latest version

  # Audio packages
  record: ^5.0.5  # Audio recording
  audioplayers: ^5.2.1 # Audio playback
  permission_handler: ^11.0.1  # Permissions management
  just_audio: ^0.10.5

  video_compress: ^3.1.4   # MediaPicker
  flutter_image_compress: ^2.4.0   # MediaPicker
  photo_view: ^0.15.0  #  DisplayMedia
  google_fonts: ^6.3.2  #  AppTheme
  curved_navigation_bar: ^1.0.6  #  personal_nav, social_nav
  provider: ^6.1.5+1  #  SettingsBottomSheet
  shared_preferences: ^2.5.3   #  SettingsManager , persistent storage
  logger: ^2.6.2  #  Logging
  flutter_dotenv: ^6.0.0  #  EnvManager
  supabase_flutter: ^2.10.3  #  Supabase service
  powersync: ^1.16.1  #  Powersync service
  firebase_core: ^4.2.0  #   firebase_notification_core
  firebase_messaging: ^16.0.3  #   firebase_notification_core,
  flutter_local_notifications: ^17.2.3 #  firebase_notification_core,  notification_types
  google_sign_in: ^6.2.1   #  Google Sign-In
  uuid: ^4.5.1  #  UUID generation
  timeago: ^3.6.1
  fl_chart: ^0.66.0 #
  emoji_picker_flutter: ^2.1.1
  photo_manager: ^3.8.3
  camera: ^0.11.3

  table_calendar: ^3.1.2

  connectivity_plus: ^7.0.0
  hive_flutter: ^1.1.0
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  device_info_plus: ^12.3.0
  package_info_plus: ^9.0.0


  screenshot: ^3.0.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.2.3


  flutter_localizations:
    sdk: flutter
  sqlite_async: ^0.13.1
  path_provider: ^2.1.5
  sqlite3_flutter_libs: ^0.5.41
  scrollable_positioned_list: ^0.3.8
  path: ^1.9.0
  url_launcher: ^6.3.1
  flutter_app_badger: ^1.5.0
  pdf: ^3.10.8               #  PDF generation (performance report)
  printing: ^5.13.1           #  PDF share/print (performance report)
dependency_overrides:
  # Force compatible versions
  record_platform_interface: ^1.1.0
  record_linux: ^1.0.0


  fl_chart: ^1.1.1
  csv: ^5.1.1
  visibility_detector: ^0.4.0+2

# dev_dependencies specify packages needed for development and testing.
dev_dependencies:
  flutter_native_splash: ^2.4.4
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1
  mocktail: ^1.0.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/images/
    - assets/chat_wallpaper/


    #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_native_splash:
  color: "#FAFAFA"
  color_dark: "#121212"
  fullscreen: true
  android_12:
    color: "#FAFAFA"
    color_dark: "#121212"
  ios: true
  android: true
  web: false

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app_logo.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/images/app_logo.png"
